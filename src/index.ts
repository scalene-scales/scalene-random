import * as ScaleneRandom from "./lib";

export type {
  TBase100Probability,
  TInitialSeed,
  TNextSeed,
  TRandomWrapper,
  TSeed,
  TSplitSeed,
} from "./types";
export { ScaleneRandom as ScaleneRandom };

export default ScaleneRandom;
